# Rust
/target
**/target

# OS
.DS_Store
Thumbs.db

# Editors
*.swp
*.bak
.idea/
.vscode/

# Node (mcp-server)
node_modules/
dist/

# Project artifacts (生成物は git 管理外)
*.wav
*.mp3
*.mid
!docs/examples/**/*.mid

# bundle SF2 (= release archive 用に build 時 download、 git-track しない / 09-distribution.md)
assets/

# Local sessions (CLAUDE.md ルール: 個人参照のみ)
HANDOFF.md
docs/sessions/

# Workspace (CODETTA_WORKSPACE は repo 外を指す前提、 万一 repo 内に作っても commit させない)
workspace/
